#these rewrite rules are intended to stop malicious requests towards Drupal sites and redirect them to the general index.php

Options +FollowSymlinks
RewriteEngine on
rewriterule ^/modules/overlay/images/utf73.php(.*)$ ^index.php$1 [r=301,nc]
